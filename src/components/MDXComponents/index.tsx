import React from 'react';
import ProviderContent from '../ProviderContent';
import ProviderVariable from '../ProviderVariable';

// MDX Components that can be used in markdown files
const MDXComponents = {
  ProviderContent,
  ProviderVariable,
};

export default MDXComponents;